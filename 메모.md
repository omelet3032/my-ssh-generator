- 이제 메시지 서명 및 공개키 출력까지 했다
- 그다음 뭘하지?
- /.ssh/ed25519에 개인키 공개키를 나누어 저장하는 로직을 만들어보자
- 따라서 다음 단계는 ed25519-dalek의 키 데이터를 OpenSSH private/public key 형식으로 어떻게 직렬화(serialization)하는지를 알아보는 게 맞아.
- ssh-key crate가 아닌 일단 ed25519-dalek을 이용해서 OpenSSH 형식으로 변환하는 연습을 해야 한다

```
③ 그걸 SSH 형식으로 포장
private_bytes
      +
public_bytes
      ↓
OpenSSH private-key format
실제 OpenSSH Ed25519 private key serialization에는 private 32바이트와 public 32바이트가 함께 들어가는 구조가 있다.
④ 파일로 저장
~/.ssh/
├── id_ed25519
└── id_ed25519.pub
이렇게 가는 게 좋아.③ 그걸 SSH 형식으로 포장
private_bytes
      +
public_bytes
      ↓
OpenSSH private-key format
실제 OpenSSH Ed25519 private key serialization에는 private 32바이트와 public 32바이트가 함께 들어가는 구조가 있다.
④ 파일로 저장
~/.ssh/
├── id_ed25519
└── id_ed25519.pub
이렇게 가는 게 좋아.